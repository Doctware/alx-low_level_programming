CC = gcc
SRC = main.c school.c
Obj = ${SRC:.c=.o}
NAME = school
RM = rm -f

all : ${NAME}

${NAME}: ${OBJ}
	${CC} ${OBJ} -o ${NAME}

clean:
	${RM} O *~ ${NAME}

oclean:
	${RM} ${OBJ}

fclean: clean oclean

re: oclean all
