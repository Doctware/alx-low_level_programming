#include <stdio.h>

/**
 * main - founction need to return an integer after execution
 *
 * Return: return 0 as sucess and 1 as fail
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
